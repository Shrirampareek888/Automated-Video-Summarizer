we'll save a main CSS file and add it to our index.html page, which is now on the same level as that of its class name but only on-
e can use an ID once in for each element; this will be called "divs" here at sublime there are two different ways you could make -
your own stylesheet or text-style:italic (or other) HTML elements like these have been created by using their names so they may n-
ot work with them all together before starting out from scratch!